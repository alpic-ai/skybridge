# Copy to .env (gitignored). Used by the notte:* scripts via --env-file-if-exists.
NOTTE_API_KEY=
# Defaults for pnpm notte:run; the --function-id / --profile-id flags override.
NOTTE_PROFILE_ID=
NOTTE_CHATGPT_FUNCTION_ID=
