# WorkOS AuthKit Configuration
AUTHKIT_DOMAIN=xxx

# Server URL — also register this as a Resource Indicator in the WorkOS
# dashboard so AuthKit issues tokens with `aud` set to it.
SERVER_URL=http://localhost:3000

# Environment
NODE_ENV=development
