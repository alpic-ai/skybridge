name: Hooks Conformance

on:
  #schedule:
  #  - cron: "0 */6 * * *"
  workflow_dispatch:
  #release:
  #  types: [published]

permissions:
  contents: read

jobs:
  conformance:
    name: ${{ matrix.host }}
    runs-on: ubuntu-latest
    timeout-minutes: 40
    strategy:
      fail-fast: false
      matrix:
        host: [chatgpt, claude]
    steps:
      - uses: actions/checkout@v7

      - uses: astral-sh/setup-uv@v6

      # The driver connects to a Notte cloud browser over CDP (--mode notte),
      # so no local browser is launched; uv resolves the script's inline deps.
      - name: Run conformance on ${{ matrix.host }}
        working-directory: examples/conformance
        run: |
          uv run notte/conformance.py \
            --host ${{ matrix.host }} \
            --mode notte \
            --profile-id ${{ vars.NOTTE_PROFILE_ID }} \
            --out conformance-results
        env:
          NOTTE_API_KEY: ${{ secrets.NOTTE_API_KEY }}

      - name: Collect mismatches
        id: results
        if: always()
        working-directory: examples/conformance
        run: |
          FILE=conformance-results/results.json
          if [ -f "$FILE" ]; then
            MISMATCHES=$(jq -r '.mismatches[]?' "$FILE" | head -10 | sed 's/^/• /')
          fi
          [ -z "${MISMATCHES:-}" ] && MISMATCHES="(no results produced — see the job logs)"
          {
            echo "mismatches<<RESULTS_EOF"
            echo "$MISMATCHES"
            echo "RESULTS_EOF"
          } >> "$GITHUB_OUTPUT"

      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: ${{ matrix.host }}-conformance-results
          path: examples/conformance/conformance-results/

      # Alert per host, inside the matrix job: matrix children collapse to one
      # shared job output, so a single downstream notify job would drop one
      # host's mismatches. Notifying here keeps each host's failure intact.
      - name: Slack Notification
        if: failure()
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_USERNAME: GitHub Actions
          SLACK_TITLE: "❌ Hooks conformance failed: ${{ matrix.host }}"
          SLACK_COLOR: failure
          SLACK_MESSAGE_ON_FAILURE: |
            The scheduled hooks conformance run failed on ${{ matrix.host }}.

            Baseline mismatches:
            ${{ steps.results.outputs.mismatches }}

            Full results (json + screenshot) are in the ${{ matrix.host }}-conformance-results artifact.
          MSG_MINIMAL: actions url
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
